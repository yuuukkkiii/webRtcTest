pluginManagement {
    repositories {
        maven {
            url "https://maven.aliyun.com/nexus/content/groups/public/"
        }
        gradlePluginPortal()
        google()
        mavenCentral()
        flatDir { dirs 'libs'}
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven {
            url "https://maven.aliyun.com/nexus/content/groups/public/"
        }
        google()
        mavenCentral()
        flatDir { dirs 'libs'}
    }
}
rootProject.name = "WebRTCTest"
include ':app'
